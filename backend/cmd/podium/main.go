// Package main is the entry point for the Podium control plane.
//
// See spec.md §1 (overview), §32 (backend structure), §39 (Dockerization),
// and AGENTS.md §3–§4 (technology rules, architecture).
package main

import "fmt"

func main() {
	fmt.Println("podium: starting")
}
